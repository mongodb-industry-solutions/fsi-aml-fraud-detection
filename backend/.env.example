# ThreatSight 360 - Fraud Detection Backend Environment Configuration
# Copy this file to .env and update with your actual values

# ==================== MONGODB CONFIGURATION ====================

# MongoDB Atlas Connection String
# Get this from your MongoDB Atlas cluster connection details
MONGODB_URI=mongodb+srv://<username>:<password>@cluster-name.xxxxx.mongodb.net/

# Database Name
# This should match the database used by the AML backend
DB_NAME=threatsight360

# ==================== AWS BEDROCK CONFIGURATION ====================

# AWS Credentials for Bedrock AI Services
# Used for fraud pattern embeddings and AI-powered risk assessment

# Option 1: Use AWS SSO (Recommended for development)
# Set AWS_USE_SSO=true and ensure you've run 'aws sso login'
# The Docker container will use mounted SSO credentials
AWS_USE_SSO=true
AWS_REGION=us-east-1

# Option 2: Use explicit credentials (for environments without SSO)
# Uncomment these lines if not using SSO
# AWS_ACCESS_KEY_ID=your_aws_access_key_here
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# ==================== SERVER CONFIGURATION ====================

# Server Host and Port
# Fraud backend runs on port 8000 (AML backend uses 8001)
HOST=0.0.0.0
PORT=8000

# Frontend URL for CORS
# Update this if your frontend runs on a different port or domain
FRONTEND_URL=http://localhost:3000

# ==================== VECTOR SEARCH CONFIGURATION ====================

# Atlas Vector Search Index Name
TRANSACTION_VECTOR_INDEX=transaction_vector_index

# ==================== IMPORTANT SECURITY NOTES ====================
#
# 1. NEVER commit this file with real credentials to version control
# 2. Use AWS IAM roles in production instead of access keys when possible
# 3. Rotate credentials regularly
# 4. Ensure MongoDB network access is properly configured
# 5. Use strong, unique passwords for all services
#
# ==================== AWS SSO SETUP INSTRUCTIONS ====================
#
# To use AWS SSO with this application:
# 1. Ensure you have AWS CLI configured with SSO: `aws configure sso`
# 2. Login to SSO: `aws sso login`
# 3. Set AWS_USE_SSO=true in your .env file
# 4. The Docker container will automatically use your SSO credentials
# 5. SSO credentials are mounted from: ~/.aws/ directory
# ThreatSight 360 - AML/KYC Backend Environment Configuration
# Copy this file to .env and update with your actual values

# ==================== MONGODB CONFIGURATION ====================

# MongoDB Atlas Connection String
# Get this from your MongoDB Atlas cluster connection details
MONGODB_URI=mongodb+srv://<username>:<password>@cluster-name.xxxxx.mongodb.net/

# Database Name
# Should match the database used by the fraud detection backend
DB_NAME=threatsight360

# ==================== AWS BEDROCK CONFIGURATION ====================

# AWS Credentials for Bedrock AI Services
# These are used for entity embedding generation and AI-powered matching
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=us-east-1

# ==================== SERVER CONFIGURATION ====================

# Server Host and Port
# AML backend runs on port 8001 to avoid conflicts with fraud backend (8000)
HOST=0.0.0.0
PORT=8001

# Frontend URL for CORS
# Update this if your frontend runs on a different port or domain
FRONTEND_URL=http://localhost:3000

# ==================== ATLAS SEARCH CONFIGURATION ====================

# Atlas Search Index Names
# These must match the index names created in MongoDB Atlas
ATLAS_SEARCH_INDEX=entity_resolution_search
ENTITY_VECTOR_INDEX=entity_vector_search_index

# ==================== IMPORTANT SECURITY NOTES ====================
# 
# 1. NEVER commit this file with real credentials to version control
# 2. Use AWS IAM roles in production instead of access keys when possible
# 3. Rotate credentials regularly
# 4. Ensure MongoDB network access is properly configured
# 5. Use strong, unique passwords for all services